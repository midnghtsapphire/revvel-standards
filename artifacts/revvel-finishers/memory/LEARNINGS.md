# Learnings Log

[Append-only]
- CLI often 4–35× fewer tokens than loading full MCP schemas for the same GitHub work.
- Finishers = deterministic audits/builds; agents only after localization.
- Budget caps per worker must be encoded in every WR.
