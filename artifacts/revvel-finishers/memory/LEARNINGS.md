# Finishers learnings (append-only)

## 2026-08-05 — Commerce last-mile is the blocker

- Generators for PDFs/zips exist; `products/dist/*` often missing because outputs are gitignored or never committed after build.
- Free full leak of R&D fleet tarball cannibalizes $399 Gumroad SKU — stop leak before/at Finisher-2.
- GitHub connector 403 on private target repos is install scope, not missing OAuth client.
- Prefer scripts (4–35× fewer tokens than full MCP schema loads for same GitHub work) — vaccine: scripts before agents.

## 2026-08-08 — Chat dumps need a deployment organizer

- Huge Grok/research chats mix peach-meme noise, Marketplace chrome, and real Finisher orders.
- WR-16924 ships `scripts/chat-deployment-organizer.js` + `products/chat-deployment-organizer` so dumps map to ORDERED_WRS without human retyping.
- Do not scramble 0→1→2; affiliate and public API wait on sales.
