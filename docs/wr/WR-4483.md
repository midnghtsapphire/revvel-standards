# WR-4483: Pipeline Code Fence Lint Fix

This work request addresses a markdownlint MD040 violation in the WR-4483
pipeline documentation by adding a `text` language tag to the fenced code
block. No content changes were made.

## Pipeline

```text
OpenRouter → OpenHands → manual (all free-tier)
```

## Notes

- Part of the per-WR lint cleanup series (1/4).
- One WR per PR.
- No functional changes; documentation-only fix.
