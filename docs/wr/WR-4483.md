# WR-4483: Pipeline Reference

One-line lint fix: add explicit `text` language to the pipeline fenced code block
to satisfy markdownlint MD040 (fenced-code-language).

## Pipeline

```text
OpenRouter → OpenHands → manual (all free-tier)
```

No content changes; language tag only.
