# WR-4483: Pipeline Code Fence Lint Fix

One-line lint fix: pipeline code fence gains a `text` language tag (markdownlint MD040).

## Pipeline

```text
issue -> agent (openrouter) -> PR -> merge -> revenue
  |
  +-> fallback: openhands -> manual
```

No content changes. Part of the per-WR lint cleanup series.
