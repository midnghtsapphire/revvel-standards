# WR-4483: Pipeline Reference

Lint cleanup: fenced code block language tags for MD040 compliance.

## Pipeline

```text
issue -> triage -> agent -> PR -> review -> merge
```

## Notes

- MD040 requires a language tag on every fenced code block.
- Use `text` for plain output or diagrams.
