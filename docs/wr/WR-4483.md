# WR-4483: Pipeline Lint Cleanup

One-line lint fix: WR-4483 pipeline code fence gains a `text` language tag (markdownlint MD040).

## Pipeline

```text
issue → agent → PR → merge → deploy
```

No content changes beyond the fence language tag.
