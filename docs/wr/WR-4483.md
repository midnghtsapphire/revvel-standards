# WR-4483: Pipeline Lint Cleanup

This work request tracks the markdownlint MD040 cleanup for the WR-4483 pipeline documentation.

## Pipeline

```text
issue -> agent -> PR -> review -> merge
```

## Notes

- Adds `text` language tag to the fenced code block to satisfy MD040.
- No content changes; lint-only fix.
- Part of the per-WR lint cleanup series (1/4).
