# Upgrade Log — every tool-tier change, dated

Append-only audit trail of every paid-tier change the pipeline makes (or
recommends). Governed by `docs/API_LIMIT_AUTO_UPGRADE.md`. **Don't garbage-
collect this file** — it's the evidence trail for the enterprise spend-control
pitch.

| Date | Tool | Trigger | Tier from → to | Monthly cost | Decision band | WR/PR | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-05-27 | Mabl | Owner request | Active → **Paused** | $X → $0 | Pause (no upgrade) | #13967 | Replaced by Keploy; eval kept in workflow header per standards. |
| 2026-05-28 | Keploy | Initial install | none → **Free** | $0 | Tier 0 (free-tier-first rule) | (install only) | App + Chrome Recorder onboarded; see `docs/TESTING_STACK.md`. |
| 2026-07-07 | CircleCI | ORBIT persona wire-in (#15406) | none → **Free** | $0 | Tier 0 (free-tier-first rule) | #15406 | Existing `.circleci/config.yml` gate formalized under ORBIT 🪐 ownership; added `validate-registries` job. Governance: `standards/CIRCLECI_INTEGRATION_STANDARD.md`. No paid tier; row added to `docs/TOOL_COST_INDEX.md`. |
| 2026-07-07 | Octopus Review | Monthly AI-usage-limit banner on PRs; OCTO persona wire-in | Hosted Free (limit hit) → **Hosted Free (unchanged)** | $0 | Tier 0 (no upgrade; lanes documented) | (this PR) | Decision deferred to owner: BYOK Anthropic/OpenAI keys in Settings (limit gone, pay provider usage), self-host (MIT; OpenRouter via gateway slots), or OSI-public (free unlimited). See `skills/octopus-expert/SKILL.md`. |
| 2026-07-07 | Mabl | MENDER persona wire-in | **Paused → Paused (unchanged)** | $0 | Pause upheld (reactivation gate documented) | (this PR) | Persona added; pause stands. New facts on record: local/CI `mabl tests run` is credit-free; mabl cloud MCP (2026-05) enables agent-driven eval at $0. Gate: E2E need Keploy+Playwright can't cover + Doppler-managed key + labeled plans. See `skills/mabl-expert/SKILL.md`. |
| 2026-08-21 | OpenRouter | Balance exhausted (402 on every call); ~$80 spent in one day with no human activity | usage-priced → **usage-priced, gated** | $0 while gated | Tier 0 (no upgrade; spend gated at the client) | #17852 / #17853 | Root cause was not the tier, it was the call volume: 46 scheduled workflows fired ~496 runs/day and ten of them called OpenRouter, ~270 billed calls/day with nobody touching the repo. Freeze: #17849 / #17851. `scripts/local_llm.py` now refuses the OpenRouter lane unless `REVVEL_LLM_ALLOW_CLOUD` is exactly `"1"`, so a sleeping laptop raises an error instead of billing. No tier change requested — do not top up credits before the per-PR reviewer fan-out (#17850) is gated, or the burn re-arms. |
| 2026-08-21 | LM Studio (Layer 0) | Layer 0 had never completed a run: cascade existed but only inside `scripts/wr_rewrite.py`, reachable from one workflow that has failed all four of its runs | none → **Local (free)** | $0 | Tier 0 (free-tier-first rule) | #17852 / #17853 | Cascade extracted to `scripts/local_llm.py` so any caller reaches Layer 0 first. Not a SaaS tier — runs on the operator's Windows laptop; setup in `docs/LOCAL_LLM_SETUP.md`. Constraint on record: GitHub-hosted runners cannot reach it, so Layer 0 pays off only for locally-run work. |
| 2026-08-21 | RecurseML | Index still showed the D007 cut 2 days after D014 reversed it | **CUT → RESTORED** (no tier change) | $0 | Tier 0 (record correction, not an upgrade) | #17855 | D014 (2026-08-19) reverses D007 (2026-07-08); `docs/TOOL_COST_INDEX.md` was never updated to match. Root cause per D014: D007 measured the `recurse-ml.yml` workflow lane, which could not post without the secret, while the GitHub App posted independently and stayed installed throughout — two delivery mechanisms conflated, and the measured one was not the running one. Guard added so a cited decision that a later decision reverses now fails the build. |
| 2026-08-21 | Devin | Absent from the index while posting a status on every PR | none → **Trial expired ($0)** | $0 | Tier 0 (record only) | #17855 | Row added. No upgrade requested — the trial is over and all 14 referencing workflows no-op. |
| 2026-08-21 | OpenRouter | Gate extended from scripts to workflows | usage-priced, gated → **usage-priced, gated everywhere** | $0 while gated | Tier 0 (no upgrade; control completed) | #17859 | Ten workflows that can bill now check the `REVVEL_LLM_ALLOW_CLOUD` repository variable. Five that reach openrouter.ai but only `GET /api/v1/models` are deliberately exempt — gating a health probe would remove the signal you need when spend is the problem. Checking endpoints first shrank the gap from fifteen workflows to ten. |
