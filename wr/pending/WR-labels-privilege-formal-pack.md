# [WR] Canonical labels, Project fields, agent privilege, formal auto-WR, disaster pack

## Description

Problem: 301 labels with duplicates; metrics stuck in labels; GitHub Projects/Models/badges underused; Claude/other guardrails stall work; sandbox wipes lose cognition; agents lack a measurable privilege ladder; formal verification does not yet open human-reviewed WRs automatically.

Desired outcome:
1. Land `config/labels-allowlist.yml` + checker workflow.
2. Document Project custom fields (Revvel Command) and badge/Models checklists.
3. Land agent reward/privilege system + scorecard script.
4. Land formal → auto WR scripts/workflow (no auto-merge).
5. Persist Grok Build formal sources + thought process under `disaster-recovery/`.
6. Prefer Actions/n8n/Make/Zapier/Gumloop over new labels (automation-first standard).

Human still reviews every PR.

## Labels
`wr`, `human-review-required`, `priority:p1`, `automation`, `area:automation`
