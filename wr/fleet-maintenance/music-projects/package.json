{
  "name": "music-projects",
  "version": "0.2.0",
  "private": true,
  "description": "Music production, songwriting, stem separation, and distribution tools hub",
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "validate:workflows": "node tests/repo-baseline.test.js --workflows-only"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
