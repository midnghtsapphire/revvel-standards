# #17790 — Option C disposition

## Decision
Agent cannot take option A/B (delete). Standard RVS-AGENT-001.
Implemented **Option C**: REVVEL-DISABLED on the three remaining root one-shots.
`patch_ossar.js` already human-deleted in #17769 — removed from RATCHET only.

## Files
- fix-semgrep.js — disabled
- fix-zizmor.js — disabled
- update_uv_lock.py — disabled
- .github/workflows/no-root-junk.yml — RATCHET shrink (drop patch_ossar.js)
- tests/no-root-junk-workflow.test.js — assert disabled + ratchet shrink

## Human still needed for full deletion (Option A)
1. Add `allow-destroy` label to a PR that deletes the three files
2. Note CircleCI `policy-check` hardcodes ALLOW_DESTROY=false — GA lane only unless that is fixed
3. Same commit: remove names from RATCHET + test lists + no-throwaway allowlist
