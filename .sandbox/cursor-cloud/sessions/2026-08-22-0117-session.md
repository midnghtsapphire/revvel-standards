# Session: WR #17736 recovery paths

Agent: cursor-cloud (bc-87d7fb26-cb4d-45a5-87a0-bcd9236748bf)
Date: 2026-08-22

## Findings

- #17822 YAML is the right shape: issue_number, force-reroute, targeted triage,
  honest reset comments. Branch is stale vs main, draft + won't-merge, dirty.
- Main already has issue_number inputs on assignee / triage / wr-pr-creation and
  reset already forwards issue_number to assignee. Recovery is STILL a no-op:
  assignee skips `openrouter`, reset does not dispatch triage, triage dispatch
  still sweeps the repo, reset comment still claims Ralph will fix it.
- #17830 (CircleCI / npm test reds) is closed via #17835. Treat #17822's
  lint-and-test red as pre-existing-on-that-base, not as a defect in the YAML.
- Vercel "Account is blocked" is owner-only; ignore.
- Out of scope: `auto-reset-stuck-issues.yml` comments "Triggered OpenRouter
  assignee workflow" but never dispatches anything.

## Decision

New branch from current main (`cursor/wr-recovery-paths-48bf`) rather than
updating Copilot's won't-merge draft. Re-apply #17822 completeness on today's
workflows. Ready-for-review PR, no won't-merge.
