# .sandbox

Per-agent scratch space and audit logs. See
`standards/VISITING_AGENT_SANDBOX_STANDARD.md` for rules.

```text
decisions.jsonl        # append-only
market_evaluator.jsonl # product creation log
```
