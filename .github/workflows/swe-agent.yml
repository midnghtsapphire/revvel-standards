name: SWE-Agent
on:
  issues:
    types:
      - labeled
  workflow_dispatch:
    inputs:
      issue_number:
        description: Issue number to resolve
        required: true
        type: number
permissions:
  contents: write
  issues: write
  pull-requests: write
concurrency:
  group: swe-agent-${{ github.event.issue.number || github.event.inputs.issue_number || github.run_id }}
  cancel-in-progress: false
jobs:
  preflight:
    name: Check SWE-agent credentials
    runs-on: ubuntu-latest
    if: |-
      (github.event_name == 'workflow_dispatch') || (github.event_name == 'issues' &&
       github.event.label.name == 'swe-fix' &&
       github.event.issue.user.login != 'github-actions[bot]')
    outputs:
      configured: ${{ steps.check.outputs.configured }}
      issue_number: ${{ steps.check.outputs.issue_number }}
    steps:
      - name: Verify required secrets
        id: check
        env:
          ISSUE_NUMBER: ${{ github.event.issue.number || github.event.inputs.issue_number }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          SWE_AGENT_API_KEY: ${{ secrets.SWE_AGENT_API_KEY }}
        run: |
          ISSUE_NUMBER="${ISSUE_NUMBER}"
          echo "issue_number=$ISSUE_NUMBER" >> "$GITHUB_OUTPUT"

          if [[ -z "$OPENROUTER_API_KEY" && -z "$SWE_AGENT_API_KEY" ]]; then
            echo "configured=false" >> "$GITHUB_OUTPUT"
            echo "::warning::Neither OPENROUTER_API_KEY nor SWE_AGENT_API_KEY is configured. See docs/agent-stack/AGENT_STACK_SETUP.md."
          else
            echo "configured=true" >> "$GITHUB_OUTPUT"
            echo "SWE-agent is configured."
          fi
    timeout-minutes: 30
  notify-missing-creds:
    name: Notify when credentials are missing
    needs: preflight
    runs-on: ubuntu-latest
    if: needs.preflight.outputs.configured == 'false'
    steps:
      - name: Post one-time setup-needed notice
        uses: actions/github-script@v9.0.0
        env:
          ISSUE_NUMBER: ${{ needs.preflight.outputs.issue_number }}
        with:
          script: |
            const issueNumber = parseInt(process.env.ISSUE_NUMBER || '', 10);
            if (!issueNumber) return;

            let comments = []; try { const { data } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              per_page: 100,
            }); comments = data; } catch (e) { console.log(`⚠️ error getting comments: ${e.message}`); }
            const marker = '<!-- swe-agent:setup-needed -->';
            if (comments.some(c => c.body && c.body.includes(marker))) {
              core.info('Setup-needed notice already posted; skipping.');
              return;
            }

            const body = [
              marker,
              '## SWE-Agent — setup needed',
              '',
              'This issue is labeled `swe-fix`, but SWE-Agent has no',
              'credentials configured. To enable autonomous code resolution:',
              '',
              '1. Add either secret to **Settings → Secrets and variables → Actions**:',
              '   - `OPENROUTER_API_KEY` (cheapest path — uses OpenRouter to access multiple models)',
              '   - `SWE_AGENT_API_KEY` (direct provider key, e.g. Anthropic or OpenAI)',
              '',
              '2. Re-apply the `swe-fix` label or comment `/retry-swe-agent`.',
              '',
              'Until then, the issue is auto-labeled `credentials-missing`.',
              'See `docs/agent-stack/AGENT_STACK_SETUP.md`.',
            ].join('\n');

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              body,
            });

            try {
              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                labels: ['credentials-missing'],
              });
            } catch (e) {
              core.warning(`Could not add credentials-missing label: ${e.message}`);
            }
    timeout-minutes: 30
  resolve:
    name: Run SWE-Agent
    needs: preflight
    runs-on: ubuntu-latest
    if: needs.preflight.outputs.configured == 'true'
    env:
      ISSUE_NUMBER: ${{ needs.preflight.outputs.issue_number }}
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
      SWE_AGENT_API_KEY: ${{ secrets.SWE_AGENT_API_KEY }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: main
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install SWE-Agent CLI
        run: |
          pip install --upgrade pip
          # Pin to a known-good release of the upstream PyPI package.
          # See: https://github.com/SWE-agent/SWE-agent
          pip install "sweagent>=1.0,<2.0"
      - name: Resolve the issue with SWE-Agent
        # Spend gate (#17850): this step bills OpenRouter. It runs only when the
        # repository variable REVVEL_LLM_ALLOW_CLOUD is set to "1". Unset = skip,
        # so an idle repo cannot spend. See docs/LOCAL_LLM_SETUP.md.
        if: vars.REVVEL_LLM_ALLOW_CLOUD == '1'
        id: resolve
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
          LLM_BASE_URL: https://openrouter.ai/api/v1
          LLM_MODEL: ${{ vars.SWE_AGENT_MODEL || 'anthropic/claude-sonnet-4.5' }}
        run: |
          set -e
          # SWE-Agent has multiple invocation modes. The GitHub-issue mode is
          # the most ergonomic for our use case.
          # See: https://swe-agent.com/latest/usage/coding_challenges/
          sweagent run \
            --agent.model.name "$LLM_MODEL" \
            --agent.model.api_base "$LLM_BASE_URL" \
            --problem_statement.github_url "https://github.com/$GH_REPO/issues/$ISSUE_NUMBER" \
            --env.repo.github_url "https://github.com/$GH_REPO" \
            --actions.open_pr true \
            --actions.pr_config.skip_if_commits_reference_issue true
      - name: Post resolver outcome
        if: always()
        uses: actions/github-script@v9.0.0
        env:
          OUTCOME: ${{ steps.resolve.outcome }}
        with:
          script: |
            const outcome = process.env.OUTCOME || 'unknown';
            const emoji = outcome === 'success' ? '✅' : '⚠️';
            const issueNumber = parseInt(process.env.ISSUE_NUMBER, 10);
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              body: [
                `${emoji} **SWE-Agent** — outcome: \`${outcome}\``,
                '',
                outcome === 'success'
                  ? 'A PR has been opened. See the linked Pull Request below.'
                  : 'SWE-Agent did not produce a clean PR. Check the [workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}) for diagnostics.',
                '',
                `_Triggered by label \`swe-fix\` on issue #${issueNumber}._`,
              ].join('\n'),
            });
    timeout-minutes: 30
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
