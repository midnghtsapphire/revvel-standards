name: SAML SSO Registration Check

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  verify-saml-identity:
    name: Verify SAML SSO Identity
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - name: Get SAML Identity
        id: saml-identity
        uses: actions/github-script@v9.0.0
        env:
          PR_AUTHOR: ${{ github.event.pull_request.user.login }}
          ORG_LOGIN: ${{ github.repository_owner }}
        with:
          github-token: ${{ secrets.ADMIN_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}
          script: |
            const login = process.env.PR_AUTHOR;
            const org = process.env.ORG_LOGIN;
            let identity = '';
            try {
              const query = `
                query($org: String!, $login: String!) {
                  organization(login: $org) {
                    samlIdentityProvider {
                      externalIdentities(first: 1, userName: $login) {
                        edges {
                          node {
                            samlIdentity { nameId }
                            user { login }
                          }
                        }
                      }
                    }
                  }
                }
              `;
              const result = await github.graphql(query, { org, login });
              const edges = result?.organization?.samlIdentityProvider?.externalIdentities?.edges || [];
              if (edges.length > 0) {
                identity = edges[0]?.node?.samlIdentity?.nameId || '';
              }
            } catch (err) {
              core.warning(`Unable to query SAML identity for ${login} in ${org}: ${err.message}`);
            }
            core.setOutput('identity', identity);
            return identity;

      - name: Report SAML identity result
        run: |
          if [ -z "${{ steps.saml-identity.outputs.identity }}" ]; then
            echo "::warning::SAML SSO identity not linked for ${{ github.event.pull_request.user.login }}"
          else
            echo "::notice::SAML SSO identity verified for ${{ github.event.pull_request.user.login }}"
          fi

      - name: Comment on PR if identity not linked
        if: steps.saml-identity.outputs.identity == ''
        uses: actions/github-script@v9.0.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const body = [
              '## SAML SSO Identity Not Linked',
              '',
              `@${context.payload.pull_request.user.login}, your GitHub account does not appear to have a linked SAML SSO identity for the \`${context.repo.owner}\` organization.`,
              '',
              'Please link your SAML identity to continue contributing. See your organization SSO settings for details.'
            ].join('\n');
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number,
              body
            });
