# Optional Polar.sh checkout for Assistant Pro upgrades.
# The core multi-agent pipeline runs fully offline without secrets.
NEXT_PUBLIC_POLAR_CHECKOUT_URL=https://polar.sh/checkout/your-personal-assistant-pro-product

# Optional future LLM enrichment (not required for runPipeline()).
# OPENROUTER_API_KEY=
