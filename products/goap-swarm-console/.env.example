# Optional Polar.sh checkout for Pro plan upsell (no secret required for free planner).
NEXT_PUBLIC_POLAR_CHECKOUT_URL=

# Optional OpenRouter key if you later wire live LLM goal decomposition.
# Name only — never commit values. See docs/SECRETS_MAP.md.
# OPENROUTER_API_KEY=
