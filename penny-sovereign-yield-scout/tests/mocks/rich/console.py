"""Unused test stub removed.

This module previously defined a minimal ``rich.console.Console`` mock, but
the test suite does not currently import it in place of the real ``rich``
package. Keeping a dead stub here is misleading, so the implementation has
been removed until test import wiring is added.
"""
