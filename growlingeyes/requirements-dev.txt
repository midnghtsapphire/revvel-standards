# GrowlingEyes — dev / test dependencies
# Install: pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
responses>=0.25.3            # Mocking requests in tests
