# GrowlingEyes Python Intelligence Tooling
# Install: pip install -r requirements.txt
# Used by: tools/ (news feeds, APT signals, stream listeners, scrapers)
#          axion_mcp/ (Axion Planetary MCP — axion_sar2optical foundation model)

# ─── News Feeds ───────────────────────────────────────────────────────────────
pygooglenews>=0.1.3          # Google News RSS wrapper (PyGoogleNews)
feedparser>=6.0.11           # General RSS/Atom/RDF feed parsing

# ─── HTTP / Async ─────────────────────────────────────────────────────────────
requests>=2.32.0
httpx>=0.27.0
aiohttp>=3.9.5

# ─── Real-Time Streams ────────────────────────────────────────────────────────
websockets>=13.0             # WebSocket client for AIS/live streams
sseclient-py>=1.8.0          # Server-Sent Events (SSE) stream consumer

# ─── HTML Scraping ────────────────────────────────────────────────────────────
beautifulsoup4>=4.12.3
lxml>=5.2.0
defusedxml>=0.7.1

# ─── Geospatial / SAR-to-Optical (axion_sar2optical) ─────────────────────────
rasterio>=1.3.11             # GeoTIFF / SAR raster I/O
numpy>=1.26.4
Pillow>=10.4.0               # Image processing and PNG output
scipy>=1.13.0                # Signal processing (speckle filtering)
pyproj>=3.6.1                # Coordinate reference system transforms

# ─── MCP Server (Axion Planetary MCP) ────────────────────────────────────────
fastmcp>=3.2.0               # FastMCP — patched (CVE fixes in 2.13+, 2.14+, 3.2+)

# ─── Scheduling / Cron ────────────────────────────────────────────────────────
APScheduler>=3.10.4          # Background job scheduler for feed polling loops

# ─── Config & Env ─────────────────────────────────────────────────────────────
python-dotenv>=1.0.1
pydantic>=2.7.0

# ─── Data ─────────────────────────────────────────────────────────────────────
pandas>=2.2.0
dateparser>=1.2.0            # Flexible date/time parsing (used by pygooglenews)

# ─── CLI Output ───────────────────────────────────────────────────────────────
rich>=13.7.0
